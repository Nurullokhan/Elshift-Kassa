# Railway va Heroku uchun sozlamalar yo'q — faqat Procfile ishlatiladi
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
